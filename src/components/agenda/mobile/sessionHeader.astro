---
import { formatTime } from "../../../helpers/agenda/agendaHelpers";
import type { AgendaSession } from "../../../types/agenda";
import Category from "../category.astro";
import TalkActions from "../talkActions.astro";

interface Props {
  sessionStartsAt: string;
  duration: number;
  sessionRoom?: string;
  session: AgendaSession;
  isSession: boolean;
}
const { sessionRoom, sessionStartsAt, duration, session, isSession } =
  Astro.props;
---

<div class="session-header">
  <Category uppercase={false} categoryColor={"var(--blue)"} track={sessionRoom} />
  <span class="time-duration">
    {formatTime(sessionStartsAt)}
    {" "}
    → {duration} min
  </span>
  {
    isSession ? (
      <TalkActions
        session={{ ...session, room: sessionRoom, startsAt: sessionStartsAt }}
      />
    ) : (
      <div />
    )
  }
</div>

<style lang="scss">
  .session-header {
    display: flex;
    flex-direction: column;
    gap: 6px;
    margin-bottom: 15px;

    .time-duration {
      font-size: 13px;
      color: var(--secondary-text-color);
      font-weight: 500;
    }
  }
</style>
