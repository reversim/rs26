---
import SocialLink from "./socialLink.astro";
import type { TeamMember } from "../types/team";
import BackButton from "./backButton.astro";
import { Picture } from "astro:assets";
import SocialLinks from "./socialLinks.astro";

interface Props extends Partial<TeamMember> {
  socialList: Record<string, string | undefined>;
  type?: "about" | "speaker";
}

const {
  socialList,
  name,
  image,
  title,
  company,
  bio,
  type = "about",
} = Astro.props;
---

<div class="main-container">
  <BackButton
    backToText={type === "about" ? "Team" : "Speakers"}
    href={type === "about" ? `/about` : `/speakers`}
  />

  <div class="person-info">
    <div class="media-socials-container">
      <Picture
        formats={["avif", "webp"]}
        src={image}
        alt={name || ""}
        width="500"
        height={"500"}
      />

      <SocialLinks
        variant="member-large"
        socialList={Object.entries(socialList || {}).map(([social, link]) => ({
          social,
          link: link || "",
        }))}
      />
    </div>
    <div class="info-container">
      <h3 class="name">{name}</h3>
      <p class="title">{title}</p>
      {company && <p class="company">{company}</p>}
      <p class="bio">{bio}</p>
    </div>
  </div>
</div>

<style lang="scss">
  .main-container {
    display: flex;
    flex-direction: column;
    gap: 24px;

    .person-info {
      display: flex;
      flex-wrap: wrap;
      gap: 42px;

      @media screen and (max-width: 768px) {
        flex-direction: column;
        justify-content: center;
        align-items: center;
        gap: 0;

        .info-container {
          padding-top: 24px;
        }
      }
    }
    .media-socials-container {
      display: flex;
      flex-direction: column;

      img {
        border-radius: 16%;
        width: 250px;
        height: 250px;
        object-fit: cover;
      }
    }

    .info-container {
      font-size: 16px;
      flex: 1;
      min-width: 300px;
      padding-top: 32px;
      font-weight: 400;

      .name {
        font-size: 28px;
        font-weight: 400;
      }

      .title {
        margin: 2px 0 8px;
      }
      .company {
        margin-bottom: 12px;
      }
      .bio {
        font-size: 14px;
        white-space: pre-wrap;
        line-height: 1.5;
      }
    }
  }
</style>
